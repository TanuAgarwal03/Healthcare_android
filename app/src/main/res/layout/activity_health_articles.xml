<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".HealthArticlesActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/healthArticles_head"
        android:text="24*7 HEALTHCARE"
        android:layout_marginTop="25dp"
        android:textStyle="bold"
        android:textColor="@color/purple_200"
        android:textSize="34sp"
        android:textAlignment="center"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/healthArticles_title"
        android:layout_below="@+id/healthArticles_head"
        android:text="Health Articles"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:textSize="22sp"
        android:textAlignment="center"
        android:layout_marginTop="10dp"/>

    <ListView
        android:layout_width="350dp"
        android:layout_height="500dp"
        android:id="@+id/healthArticles_listView"
        android:layout_below="@id/healthArticles_title"
        android:layout_marginTop="30dp"
        android:layout_marginHorizontal="30dp"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/healthArticles_backBtn"
        android:text="BACK"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="150dp"
        android:layout_below="@id/healthArticles_listView"/>

</RelativeLayout>
